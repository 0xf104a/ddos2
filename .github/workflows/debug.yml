name: build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: docker://gcc:9
    - name: build-debug
      run: |
        ls
        pwd
        ./build.sh clean
        ./build.sh debug
        ./build.sh modules
