name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: docker://gcc:9.0
    - name: build-debug
      run: |
        ./build.sh clean
        ./build.sh debug
        ./build.sh modules
