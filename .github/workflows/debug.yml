name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1!
    - name: build-debug
      run: |
        ./build.sh clean
        ./build.sh debug
        ./build.sh modules
